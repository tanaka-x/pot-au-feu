{
  "name": "pot-au-feu",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dbmock": "cd ./portfoward_testdocker/ && docker-compose build && docker-compose up -d",
    "ncu:update": "ncu -u && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-log": "^4.2.4",
    "spectre.css": "^0.5.9",
    "tunnel-ssh": "^4.1.4"
  },
  "devDependencies": {
    "electron": "^9.3.1",
    "electron-builder": "^22.8.0",
    "npm-check-updates": "^7.1.1"
  },
  "build": {
    "appId": "com.electron.worc",
    "win": {
      "target": "dmg"
    },
    "directories": {
      "output": "docs"
    }
  }
}
